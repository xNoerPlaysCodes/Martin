#include <martin/bot.hpp>
#include <iostream>

namespace martin
{
    int move(std::array<std::array<char, 8>, 8> &board)
    {
        std::cout << "Bot got a chance to move\n";
        return 0;
    } // TODO: Fix

    bool check(const std::array<std::array<char, 8>, 8> &board)
    {
        // TODO: Fix
        return true;
    }
}
